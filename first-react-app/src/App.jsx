function App() {
  let content = <h1>Hello React World</h1>;
  // We can only return a single tag, thus we'll be wrapping all our HTML within empty fragments -> <>...</>
  return content;
}

export default App;